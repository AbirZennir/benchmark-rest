spring:
  application:
    name: variant-c-springmvc

# Commun à tous les profils
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    prometheus:
      enabled: true

---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5432/benchmarkdb
    username: benchmark
    password: benchmark
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate.format_sql: true
      hibernate.jdbc.batch_size: 50
      hibernate.cache.use_second_level_cache: false
      hibernate.cache.use_query_cache: false

---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:postgresql://benchmark-db:5432/benchmarkdb
    username: benchmark
    password: benchmark
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate.format_sql: true
      hibernate.jdbc.batch_size: 50
      hibernate.cache.use_second_level_cache: false
      hibernate.cache.use_query_cache: false
